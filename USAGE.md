# 如何使用 AI 监工+干活系统

## 快速开始

### 1. 安装依赖

```bash
pip install openai
```

### 2. 配置 API Key

```bash
export OPENAI_API_KEY="your-openai-api-key"
```

### 3. 运行

```bash
python ai_team.py
```

## 工作流程

```
你 → 描述项目需求
     ↓
监工 AI (Claude Code/我) → 制定任务计划
     ↓
干活 AI (GPT-4) → 编写代码
     ↓
监工 AI → 审核代码
     ↓
✅ 通过 → 保存文件
❌ 不通过 → 标记需要重做
```

## 角色说明

**监工 (Claude Code)**
- 分析项目需求
- 制定任务清单
- 审核代码质量
- 决定是否通过

**干活 (GPT-4)**
- 接收具体任务
- 编写代码实现
- 只负责干活，不做决策

## 示例输出

```
============================================================
🚀 AI 团队开始工作
============================================================

📋 [监工] 正在分析项目并制定计划...
项目需求: 创建待办事项管理器

✅ [监工] 已制定 3 个任务
  任务 1: 创建主程序文件，包含基本的类结构
  任务 2: 创建工具函数文件
  任务 3: 创建单元测试文件

------------------------------------------------------------

⚙️  [干活] 正在执行任务 1: 创建主程序文件
✅ [干活] 完成编码，共 45 行

🔍 [监工] 正在审核任务 1 的代码...
✅ [监工] 审核通过！评分: 10/10
💾 [系统] 代码已保存到: workspace/main.py

============================================================
🎉 工作完成！3/3 个任务通过审核
============================================================
```

## 自定义项目

修改 `ai_team.py` 中的 `project` 变量：

```python
project = """
你的项目描述...
"""
```

## 高级配置

### 更换干活 AI

```python
# 使用其他模型
self.worker_client = openai.OpenAI(...)
# 在 worker_execute 中改为:
model="gpt-3.5-turbo"  # 更便宜但质量稍低
```

### 自定义审核规则

在 `supervisor_review()` 函数中添加你的检查逻辑：

```python
# 检查代码风格
if not code.startswith('"""'):
    issues.append("缺少文档字符串")

# 检查安全性
if "eval(" in code or "exec(" in code:
    issues.append("使用了不安全的函数")
```

## 文件结构

```
AI-cloud-project/
├── ai_team.py              # 主程序
├── workspace/              # 生成的代码存放处
│   ├── main.py
│   ├── utils.py
│   └── test_main.py
└── USAGE.md               # 本文件
```

## 注意事项

1. **API 费用**: GPT-4 调用会产生费用，建议先用小项目测试
2. **审核准确性**: 当前是简单的规则检查，可以升级为调用 Claude API 做深度审核
3. **错误处理**: 如果干活 AI 生成的代码有问题，监工会标记为失败
4. **重试机制**: 当前版本不会自动重试，失败的任务需要手动处理

## 下一步改进

- [ ] 添加自动重试机制（让干活 AI 修复问题）
- [ ] 监工使用 Claude API 做深度代码审核
- [ ] 支持多个干活 AI 并行工作
- [ ] 添加任务优先级和依赖关系
- [ ] 集成代码格式化工具 (black, pylint)
